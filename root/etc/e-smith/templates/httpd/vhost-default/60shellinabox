#
#60shellinabox
#
{
my $name = ${'shellinaboxd'}{'Name'} || 'shell';

if ( $Port == 80 || ! defined $SslMode )
{
$OUT .= " RewriteEngine On\n";
$OUT .= " RewriteRule ^/$name(/.*|\$)    https://%{HTTP_HOST}/$name\$1 [L,R]\n";
}
}
